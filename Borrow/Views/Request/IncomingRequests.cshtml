@model UserBorrowRequestsViewModel

<h1>Here are your incoming Requests</h1>
<div>
    <div>
        <table style="width:100%">
            <tr>
                <th>Item</th>
                <th>Requested Created</th>
                <th>Requested Rate</th>
                <th>Counter Rate</th>
                <th>Status</th>
                <th>Action</th>
                <th></th>
            </tr>
            @for (int i = 0; i < Model.Incoming.Count(); ++i)
            {
                var item = Model.Incoming[i];
                <tr>
                    <td>
                        @item.ItemName
                    </td>
                    <td>
                        @item.CreatedDateUtc
                    </td>
                    <td>
                        @item.RateView
                    </td>
                    <td>
                        @item.CounterRateView
                    </td>
                    <td>
                        @item.Status
                    </td>

                    <td>
                        @if (item.RequesterNeedsAction())
                        {
                            if (item.Status.Equals(Request.RequestStatus.RequesterCounter))
                            {
                                @using (Html.BeginForm("ViewCounterOffer", "Request", FormMethod.Get))
                                {
                                    <button class="btn btn-primary" type="submit" name="requestId" value=@item.RequestId>View Requester's Counter Offer</button>
                                }
                            }
                            else if (item.Status.Equals(Request.RequestStatus.Accepted))
                            {
                                @using (Html.BeginForm("MeetUpSpot", "MeetUp", FormMethod.Get))
                                {
                                    <button class="btn btn-primary" type="submit" name="requestId" value=@item.RequestId>Meet</button>
                                }
                            }
                        }
                        else
                        {
                            @using (Html.BeginForm("ViewRequestInfo", "Request", FormMethod.Get))
                            {
                                <button class="btn btn-primary" type="submit" name="requestId" value=@item.RequestId>View Request Information</button>
                            }
                        }
                    </td>
                </tr>
            }
        </table>
    </div>
</div>